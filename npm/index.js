// Stub file - the actual binary is installed by postinstall script
// This file exists to satisfy npm's bin field requirements

console.log('RustDex should be called directly via the installed binary.');
console.log('If you see this message, the installation may not have completed correctly.');
console.log('Please run: npm install -g rustdex --force');

process.exit(1);